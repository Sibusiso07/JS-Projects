<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css">
    <title>Weather API</title>
</head>
<body>
    <h1>City Weather App</h1>
    <div class="container">
        <div class="response-area">
            <!-- display data -->
            <% if (locals.location) { %>
                <h3><%= location %></h3>
            <% } else { %>
                <h3>Sandton</h3>
            <% } %>
                <p>
                    Temperature: <%= tempData %>°C<br>
                    Type: <%= typeData %> skies<br>
                    Humidify: <%= humidityData %>%<br>
                    Wind: <%= windSpeed %><br>
                    Date: <small><%= new Date().toJSON().slice(0, 10); %></small>
                </p>
        </div>
        <form id="myForm">
            <label for="chosenCity">City:</label>
            <input type="text" id="chosenCity" name="city">

            <label for="submit">Search: </label>
            <input id="get" type="submit" value="SEARCH" formaction="/result">
        </form>
    </div>
</body>
</html>